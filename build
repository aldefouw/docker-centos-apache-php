#/bin/bash

read -p "Docker namespace (naqoda): " namespace
namespace=${namespace:-naqoda}

docker build -t $namespace/centos-apache-php .

tag=$(git symbolic-ref --short HEAD)

if [[ $tag == 'master' ]]; then
	tag='latest'
fi

docker push $namespace/centos-apache-php:$tag 	